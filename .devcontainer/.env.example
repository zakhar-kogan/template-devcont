# =============================================================================
# RUNTIME CONFIGURATION
# =============================================================================
# This file is for RUNTIME environment variables only.
# Build-time variables (versions, UID/GID) are configured in devcontainer.json.
#
# Loaded automatically via runArgs --env-file.

# =============================================================================
# FIREWALL
# =============================================================================

# Set to false to disable egress restrictions
ENABLE_FIREWALL=false

# Comma-separated list of additional domains to allow
FIREWALL_EXTRA_DOMAINS=

# =============================================================================
# AI PROXY CONFIGURATION
# =============================================================================
# Set these to redirect supported AI coding tools (Claude, OpenCode, Droid)
# to a custom API endpoint.
#
# AI_PROXY_BASE_URL=http://host.docker.internal:8317
# AI_PROXY_API_KEY=your-proxy-api-key
#
# Tool-specific overrides (optional - falls back to AI_PROXY_* if not set):
# ANTHROPIC_BASE_URL=http://host.docker.internal:8317
# ANTHROPIC_AUTH_TOKEN=your-anthropic-token
# OPENCODE_BASE_URL=http://host.docker.internal:8317/v1
# OPENCODE_API_KEY=your-opencode-key
# DROID_BASE_URL=http://host.docker.internal:8317
# DROID_API_KEY=your-droid-key
# DROID_PROVIDER=anthropic  # anthropic, openai, or generic-chat-completion-api
